{
  "name": "carshake",
  "version": "1.0.0",
  "private": true,
  "functions": {
    "api/email-capture.js": {
      "maxDuration": 10
    }
  },
  "scripts": {
    "prerender": "node prerender.mjs",
    "guard:positioning": "node scripts/guard-positioning.mjs",
    "deploy": "npm run prerender && node scripts/guard-positioning.mjs && npx vercel --prod --yes"
  }
}
