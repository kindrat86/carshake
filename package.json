{
  "name": "carshake",
  "version": "1.0.0",
  "private": true,
  "functions": {
    "api/email-capture.js": {
      "maxDuration": 10
    }
  },
  "scripts": {
    "prerender": "node prerender.mjs",
    "guard:positioning": "node scripts/guard-positioning.mjs",
    "deploy": "npm run prerender && python3 \"$HOME/.growth-engine/inject-disambiguation.py\" carshake && node scripts/guard-positioning.mjs && npx vercel --prod --yes"
  }
}
